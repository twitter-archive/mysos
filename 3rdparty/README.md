Place any bleeding edge versions of packages here so that they may be picked up by tox during
testing.

This is also the place for the required `mesos.native` packages because they are platform specific.
